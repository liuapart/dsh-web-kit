// ============================================================================
// @apanoo/dsh-web-kit — 服务端半部入口（host plugin，ESM 由打包器生成到 lib/index.js）
// ----------------------------------------------------------------------------
// v2.6.0：/web-kit 文件读取路由已随功能一（文件预览停靠栏）下线——
// dsh 0.1.5 官方新增右侧文件预览栏，能力重叠，且该路由暴露任意路径读取面
// （fence.js 的信任围栏 + 文件系统权限），无消费者后应尽快消失。
// 服务端现无职责；保留插件骨架维持 loader entry 稳定。旧实现见 git 历史。
const name = "web-kit";
const inject = [];

/** dsh 生命周期入口：当前无服务端职责。 */
function apply(ctx) {
	// no-op（v2.6.0 移除 /web-kit 路由注册）
}

module.exports = { name, inject, apply };
